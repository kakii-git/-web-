# .env.example
# このファイルをコピーして .env を作成してください
# コマンド例: cp .env.example .env

# --- Database Settings ---
# PostgreSQLのデフォルトユーザー (通常は "postgres")
DB_USER=postgres

# docker-compose.yml の POSTGRES_PASSWORD と同じ値に設定してください
# ローカル開発用の初期値として mypassword を入れています
DB_PASSWORD=mypassword

# docker-compose.yml のサービス名 (通常は "db")
DB_HOST=db
DB_PORT=5432

# docker-compose.yml の POSTGRES_DB と同じ値に設定してください
DB_NAME=myapp_db

# --- Security ---
# JWT署名などに使用する秘密鍵
# 本番環境では "openssl rand -hex 32" 等で生成した安全な値に変更してください
SECRET_KEY=dev_secret_key_change_me

# --- Slack Integration ---
# Slack App の設定画面から取得した値を入力してください
# 連携機能を使用しない場合は空欄でも可
SLACK_CLIENT_ID=
SLACK_CLIENT_SECRET=
SLACK_REDIRECT_URI=

# --- CORS Settings (Frontend Connection) ---
# フロントエンドからのアクセスを許可するオリジン (JSON形式のリスト)
BACKEND_CORS_ORIGINS=["http://localhost:3000", "http://127.0.0.1:3000"]